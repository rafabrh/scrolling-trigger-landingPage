node_modules/
.next/
out/
.DS_Store
*.log
.env*
!.env.example

# A sequencia de frames E versionada, de proposito. Gera-la no container de
# build custaria 616 MB de disco temporario e 360 encodes do sharp a cada
# deploy, para produzir exatamente os mesmos 23 MB. O `pnpm frames` continua
# sendo a ferramenta de regeneracao quando o video mudar.

# Payload de 2 MB gerado pelo canvas de design. As fontes são os .dc.html.
design-canvas/shk-group-site.html

.idea/

# Diretorio temporario do pipeline de frames. O build-frames.mjs apaga no fim,
# mas ele fica no disco enquanto o script roda e nao pode vazar para o commit.
.frames-tmp/

# Cache incremental do tsc.
*.tsbuildinfo

# Gerados pelo `next dev`. Desligados via agentRules no next.config.ts.
AGENTS.md
CLAUDE.md
